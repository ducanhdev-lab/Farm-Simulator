# Imported assets

Drop CC0 / properly licensed FBX/GLTF models here.

Suggested replacements (portfolio):

1. **Player** — swap mesh on `Player` prefab (keep CharacterController + scripts).
2. **One animal** — e.g. `Alpaca` or `Chicken` prefab.
3. **App icon** — replace `Assets/_Game/Textures/Icon.png` (1024×1024).
4. **Title** — replace `Assets/_Game/Sprites/MainMenu/GameTitle.png`.

After import, add a row to `/CREDITS.md`.
